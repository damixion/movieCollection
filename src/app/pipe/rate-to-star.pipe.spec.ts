import { RateToStarPipe } from './rate-to-star.pipe';

describe('RateToStarPipe', () => {
  it('create an instance', () => {
    const pipe = new RateToStarPipe();
    expect(pipe).toBeTruthy();
  });
});
